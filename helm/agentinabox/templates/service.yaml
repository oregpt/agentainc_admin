apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentinabox.fullname" . }}
  labels:
    {{- include "agentinabox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentinabox.selectorLabels" . | nindent 4 }}
